pipeline {
    agent any
      environment {
        DOCKERHUB_CREDENTIALS = credentials('SAMAR')
    }

    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/samarchieb/devops', branch: 'main'
            }
        }
        stage('Init'){
            steps{
                bat 'echo $DOCKERHUB_CREDENTIALS_PSW | docker login -u $DOCKERHUB_CREDENTIALS_USR --password-stdin'
            }
        }

        
      
        stage('Build') {
            steps {
                bat 'cd Front end (Angular) && docker build -t samarchieb/frontend:latest .'
                bat 'cd Back end (SpringBoot) && docker build -t samarchieb/backend:latest .'
            }
        }
        stage('Deliver') {
            steps {
                bat 'docker push samarchieb/frontend:latest'
                bat 'docker push samarchieb/backend:latest'
            }
        }
          stage('Cleanup') {
            steps {
                bat 'docker system prune -f'
               // bat 'docker rmi samarchieb/frontend:latest'
               // bat 'docker rmi samarchieb/backend:latest'
                bat 'docker logout'
            }
        }
         
    }

}